;   Define global symbols and possibly additional build-time setup required by
;   the IIC library module.  This file is intended to be included from the main
;   include file of a project using the IIC library module.
;
/flag    iicack              ;set on ACK received
